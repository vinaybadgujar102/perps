// import { USERS } from "./user.util";

export function lockUserMargin(userId: number, amount: number): void {
  void userId;
  void amount;
  // const user = USERS.getUser(userId);
  // if (!user) return;
  // user.lockedBalance += amount;
}

export function unlockUserMargin(userId: number, amount: number): void {
  void userId;
  void amount;
  // const user = USERS.getUser(userId);
  // if (!user) return;
  // user.lockedBalance = Math.max(0, user.lockedBalance - amount);
}
