# API (optional — defaults work for local dev)
# VITE_API_BASE_URL=/api/v1
# VITE_API_PROXY_TARGET=http://localhost:3003

# Hosted demo: keep chart on static fake candles (no live WS bar updates)
# VITE_HOSTED_DEMO=true
